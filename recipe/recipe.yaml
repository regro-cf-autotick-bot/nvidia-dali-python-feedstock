# TODO: Update package pinnings in conda_build_config for every release
schema_version: 1
context:
  version: "2.3.0"

recipe:
  name: nvidia-dali
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA/DALI/archive/refs/tags/v${{ version }}.tar.gz
    sha256: 4a54e22e13e713aed35e7eb8a81410a509a48491956534bcb46ccd8f70e25200
    patches:
      - patches/0001-BLD-Use-CUDA-target-include-directory-to-support-cro.patch
      - patches/0002-DOC-Patch-dlopen-failure-message-and-dlopen-search-p.patch
      - patches/0003-BLD-Use-shared-libtar-instead-of-static.patch
      - patches/0007-BLD-Use-pre-build-cvcuda-instead-of-submodule.patch
  - url: https://github.com/cocodataset/cocoapi/archive/8c9bcc3cf640524c4c20a9c40e89cb6a2f2fa0e9.tar.gz
    sha256: 4dd3450bab2287d1c1b271cf4a1420db05294194f0ad9af4e3af592d6f2b4410
    target_directory: third_party/cocoapi
  - url: https://github.com/JanuszL/ffts/archive/6ef8d818f46f679c95110ab199ae915fb04bfef5.tar.gz
    sha256: 1d157157828ce1c85d0a43a6de312ce168b3c5dc1223c6ad3f02e074c3806c15
    target_directory: third_party/ffts

build:
  number: 0
  skip:
    - cuda_compiler_version == "None"
    - win
    - osx

outputs:
  - staging:
      name: core-build
    build:
      script: build.sh
    requirements:
      build:
        - ${{ compiler('cuda') }}
        - ${{ compiler('cxx') }}
        - ${{ stdlib('c') }}
        - if: linux and aarch64 and not match(cuda_compiler_version, "==None")
          then:
            - arm-variant * ${{ arm_variant_type | default("None") }}
        - cmake 3.*
        - file
        - ninja
        - pkgconf
        - python-clang
        - if: build_platform != target_platform
          then:
            - libprotobuf
            - libprotobuf-static
      host:
      # NOTE: static libraries culibos required for correct feature detection
        - if: linux and match(cuda_compiler_version, "13.*")
          then:
            - cuda-culibos-static
        - cuda-cudart-dev
        - cuda-cudart-static
        - cuda-nvml-dev
        - libcufft-dev
        - libcufile-dev
        - libcurand-dev
        - libnpp-dev
        - libcvcuda-dev
        - if: (arm_variant_type | default("")) != "tegra"
          then:
            - libnvcomp-dev  ${{ libnvcomp }}
        - libnvimgcodec-dev  ${{ libnvimgcodec }}
        - cuda-version ${{ cuda_compiler_version }}.*
        - aws-sdk-cpp
        - cfitsio
        - cutlass
        - dlpack
        - ffmpeg
        - ffmpeg * lgpl*
        - libabseil
        - libboost-headers  ${{ libboost_headers }}.*
        - libjpeg-turbo
        - libopencv
        - libprotobuf
        - libprotobuf-static
        - libsndfile
        - libtar
        - lmdb
        - nvtx-c
        - rapidjson
        - zlib

  - package:
      name: libdali
      version: ${{ version }}
    inherit:
      from: core-build
      run_exports: true
    build:
      script: build.sh
      files:
        - lib/libdali*.so
    requirements:
      ignore_run_exports:
        by_name:
          - cuda-version
        from_package:
          - libcufft-dev
          - libcufile-dev
          - libcurand-dev
          - libnpp-dev
          - libnvimgcodec-dev
          - cuda-cudart-dev
          - if: (arm_variant_type | default("")) != "tegra"
            then:
              - libnvcomp-dev
          # Added libprotobuf to pick up channel pinnings, but we use the static package
          - libprotobuf
      build:
        - ${{ compiler('cuda') }}
        - ${{ compiler('cxx') }}
        - ${{ stdlib('c') }}
        - if: linux and aarch64 and not match(cuda_compiler_version, "==None")
          then:
            - arm-variant * ${{ arm_variant_type | default("None") }}
        - cmake 3.*
        - file
        - ninja
        - pkgconf
      host:
        - cuda-version ${{ cuda_compiler_version }}.*
        - libcvcuda-dev
      run:
        - ${{ pin_compatible('cuda-version', lower_bound='x', upper_bound='x') }}
        - libnvimgcodec ${{ libnvimgcodec }}
        - libnvimgcodec-libjpeg-turbo-ext ${{ libnvimgcodec }}
        - libnvimgcodec-libtiff-ext       ${{ libnvimgcodec }}
        - libnvimgcodec-libopencv-ext     ${{ libnvimgcodec }}
        - if: (arm_variant_type | default("")) != "tegra"
          then:
            - libnvcomp  ${{ libnvcomp }}
      run_constraints:
        - libboost-headers  ${{ libboost_headers }}.*
    tests:
      - script:
          - test -f "${PREFIX}/lib/libdali.so"
          - test -f "${PREFIX}/lib/libdali_core.so"
          - test -f "${PREFIX}/lib/libdali_kernels.so"
          - test -f "${PREFIX}/lib/libdali_operators.so"
          # Negative check: headers live in libdali-devel now, not here.
          - test ! -f "${PREFIX}/include/dali/c_api.h"
    about:
      summary: DALI native GPU-accelerated data loading libraries (CUDA runtime, Python-independent).
      description: |
        Native shared libraries for NVIDIA DALI. Contains the core GPU-accelerated
        processing engine. Install nvidia-dali-python for the Python API.
      license: Apache-2.0 AND BSD-3-Clause AND BSD-2-Clause
      license_file:
        - LICENSE
        - COPYRIGHT
        - Acknowledgements.txt  # Contains licenses for linked libraries
        - third_party/README.rst
      homepage: https://github.com/NVIDIA/dali

  - package:
      name: libdali-devel
      version: ${{ version }}
    inherit:
      from: core-build
      run_exports: false
    build:
      script: build.sh
      files:
        - include/dali/**
    requirements:
      build:
        - ${{ compiler('cuda') }}
        - ${{ compiler('cxx') }}
        - ${{ stdlib('c') }}
        - if: linux and aarch64 and not match(cuda_compiler_version, "==None")
          then:
            - arm-variant * ${{ arm_variant_type | default("None") }}
        - cmake 3.*
        - file
        - ninja
        - pkgconf
      host:
        - cuda-version ${{ cuda_compiler_version }}.*
      run:
        - ${{ pin_compatible('cuda-version', lower_bound='x', upper_bound='x') }}
        - ${{ pin_subpackage('libdali', exact=True) }}
      run_exports:
        - ${{ pin_subpackage('libdali', upper_bound="x.x.x") }}
    tests:
      - script:
          - test -f "${PREFIX}/include/dali/c_api.h"
          - test -f "${PREFIX}/include/dali/core/common.h"
          - test -f "${PREFIX}/include/dali/pipeline/dali.pb.h"
    about:
      summary: DALI development headers (C++ API).
      description: |
        Development headers for NVIDIA DALI. Required for building software
        that links against libdali. Install libdali for the runtime libraries.
      license: Apache-2.0 AND BSD-3-Clause AND BSD-2-Clause
      license_file:
        - LICENSE
        - COPYRIGHT
        - Acknowledgements.txt  # Contains licenses for linked libraries
        - third_party/README.rst
      homepage: https://github.com/NVIDIA/dali

  - package:
      name: nvidia-dali-python
      version: ${{ version }}
    inherit:
      from: core-build
      run_exports: false
    build:
      script: build.sh
    requirements:
      ignore_run_exports:
        by_name:
          - cuda-version
        from_package:
          # Added libprotobuf to pick up channel pinnings, but we use the static package
          - libprotobuf
          - zlib
          - libcufft-dev
          - libcufile-dev
          - libcurand-dev
          - libnpp-dev
          - libnvimgcodec-dev
          - cuda-cudart-dev
          - if: (arm_variant_type | default("")) != "tegra"
            then:
              - libnvcomp-dev
      build:
        - ${{ compiler('cuda') }}
        - ${{ compiler('cxx') }}
        - ${{ stdlib('c') }}
        - if: linux and aarch64 and not match(cuda_compiler_version, "==None")
          then:
            - arm-variant * ${{ arm_variant_type | default("None") }}
        - cmake 3.*
        - file
        - ninja
        - pkgconf
        - if: build_platform != target_platform
          then:
            - cross-python_${{ target_platform }}
            - python
            - python-clang
            - libprotobuf
            - libprotobuf-static
      host:
        - ${{ pin_subpackage('libdali-devel', upper_bound="x.x.x") }}
          # NOTE: static libraries culibos and nvjpeg required for correct feature detection
        - if: linux and match(cuda_compiler_version, "13.*")
          then:
            - cuda-culibos-static
        - cuda-cudart-dev
        - cuda-cudart-static
        - cuda-nvml-dev
        - cuda-version ${{ cuda_compiler_version }}.*
        - libboost-headers  ${{ libboost_headers }}.*
        - if: (arm_variant_type | default("")) != "tegra"
          then:
            - libnvcomp-dev  ${{ libnvcomp }}
        - libnvimgcodec-dev  ${{ libnvimgcodec }}
        - libcvcuda-dev
        - cutlass
        - dlpack
        - libprotobuf
        - libprotobuf-static
        - nvtx-c
        - rapidjson
        - zlib
        - pip
        - pybind11
        - python
        - python-clang
        - setuptools
        # Runtime deps also needed at host time for stub generator
        - nvtx
        - makefun
        - astunparse ${{ astunparse }}
        - gast ${{ gast }}
        - six ${{ six }}
        - optree
        - libcst ${{ libcst }}
        - numpy
        - packaging ${{ packaging }}
        # Stubs autoformat themselves
        - black  ${{ black }}
      run:
        - ${{ pin_subpackage('libdali', upper_bound="x.x.x") }}
        - python
        - nvtx
        - makefun
        - astunparse ${{ astunparse }}
        - gast ${{ gast }}
        - six ${{ six }}
        - optree
        - libcst ${{ libcst }}
        - packaging ${{ packaging }}
        - ${{ pin_compatible('cuda-version', lower_bound='x', upper_bound='x') }}
    tests:
      - requirements:
          run:
            - pip
            # Test that optional dependencies are co-installable
            - libcufft
            - libcufile
            - libnpp
            - libboost-headers
            - if: (arm_variant_type | default("")) != "tegra"
              then:
                - libnvcomp
            - libnvimgcodec
            - libnvimgcodec-libjpeg-turbo-ext
            - libnvimgcodec-libtiff-ext
            - libnvimgcodec-libopencv-ext
        script:
          - pip check
          - python -c "import nvidia.dali"
          - |
            python <<'PY'
            import glob
            import json
            import os

            metadata = glob.glob(os.path.join(os.environ["PREFIX"], "conda-meta", "nvidia-dali-python-*.json"))
            if len(metadata) != 1:
                raise SystemExit(f"Expected one nvidia-dali-python metadata file, found: {metadata}")
            with open(metadata[0], encoding="utf-8") as fh:
                files = set(json.load(fh)["files"])

            native_libs = {
                "lib/libdali.so",
                "lib/libdali_core.so",
                "lib/libdali_kernels.so",
                "lib/libdali_operators.so",
            }
            unexpected = sorted(
                path
                for path in files
                if path.startswith("include/dali/")
                or path.startswith("lib/python") and "/site-packages/nvidia/dali/include/" in path
                or path in native_libs
            )
            if unexpected:
                raise SystemExit("nvidia-dali-python owns native files: " + ", ".join(unexpected[:20]))
            PY
          - tfrecord2idx --help
          - wds2idx --help
    about:
      summary: A GPU-accelerated library for data loading and pre-processing to accelerate deep learning applications.
      description: |
        A GPU-accelerated library containing highly optimized building blocks and
        an execution engine for data processing to accelerate deep learning training
        and inference applications.
      license: Apache-2.0 AND BSD-3-Clause AND BSD-2-Clause
      license_file:
        - LICENSE
        - COPYRIGHT
        - Acknowledgements.txt  # Contains licenses for linked libraries
        - third_party/README.rst
      homepage: https://github.com/NVIDIA/dali

about:
  summary: A GPU-accelerated library for data loading and pre-processing to accelerate deep learning applications.
  license: Apache-2.0 AND BSD-3-Clause AND BSD-2-Clause
  license_file:
    - LICENSE
    - COPYRIGHT
    - Acknowledgements.txt  # Contains licenses for linked libraries
    - third_party/README.rst
  homepage: https://github.com/NVIDIA/dali

extra:
  feedstock-name: nvidia-dali-python
  recipe-maintainers:
    - conda-forge/cuda
    - isuruf
    - timkpaine
